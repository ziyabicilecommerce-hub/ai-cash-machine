---
description: Multi-agent collaboration plugin that spawns N parallel subagents competing on the same task via git worktree isolation. Agents work independently, results are evaluated by metric or LLM judge, and the best branch is merged. Use when: user wants multiple approaches tried in parallel — code optimiz
---

Apply the **agenthub** skill to the request below. Follow the full instructions in `.claude/skills/agenthub/SKILL.md`.

Request: $ARGUMENTS
