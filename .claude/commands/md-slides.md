---
description: Converts a markdown deck (slides separated by `---` HR boundaries or by `# ` H1 headings, with optional `<!-- notes: ... -->` presenter notes blocks) into a single-file HTML presentation with arrow-key / space / PgDn / PgUp / Home / End / P / Esc keyboard navigation, presenter mode (split view with 
---

Apply the **md-slides** skill to the request below. Follow the full instructions in `.claude/skills/md-slides/SKILL.md`.

Request: $ARGUMENTS
