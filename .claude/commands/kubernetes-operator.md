---
description: Use when building a Kubernetes Operator — custom controllers that reconcile CRD state. Triggers on "build an operator", "CRD design", "reconcile loop", "controller-runtime", "kubebuilder", "operator-sdk", "metacontroller", "KOPF", "operator capability levels", or "custom resource". Ships CRD valid
---

Apply the **kubernetes-operator** skill to the request below. Follow the full instructions in `.claude/skills/kubernetes-operator/SKILL.md`.

Request: $ARGUMENTS
