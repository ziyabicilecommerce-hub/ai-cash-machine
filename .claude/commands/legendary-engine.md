---
name: legendary-engine
description: Runs the real Legendary Engine — generates an actual world-class marketing battle plan (headlines, Grand Slam Offer with value math, value ladder, email sequence, viral concepts, conversion projections) from a product brief. Real generated output, not instructions.
---

# 👑 LEGENDARY ENGINE — Real Output Generator

This is not a prompt agent — it runs real code that GENERATES marketing assets from a product brief, implementing the frameworks of agents 151-160.

## Input from user: $ARGUMENTS

Collect (ask if missing): product name, audience, dream outcome, price, pain point, current conversion rate, monthly traffic.

## How to Run

Build a JSON brief and execute the engine:

```bash
node .agents/skills/legendary-engine/engine.js '{"product":"NAME","audience":"WHO","dreamOutcome":"RESULT","painPoint":"PAIN","price":497,"currentRate":0.008,"traffic":12000}'
```

Run with no argument to see the built-in AI Cash Machine demo:
```bash
node .agents/skills/legendary-engine/engine.js
```

## What It Generates (real, computed)
- 🧭 Positioning statement (Agent 157)
- ✍️ 25 headlines ranked by pull (Agent 152)
- 🏆 Grand Slam Offer with value-stack math + guarantees (Agent 151)
- 🎬 Value ladder with computed price points (Agent 154)
- 📧 7-email launch sequence (Agent 154)
- 🦠 STEPPS viral concepts (Agent 158)
- 📊 Conversion projections with revenue math (Agent 160)

## Your Job
1. Gather the brief from the user
2. Run the engine with their real numbers
3. Present the generated battle plan
4. Then ELEVATE it — take the raw generated assets and refine the top picks with your own judgment (the engine gives the skeleton; you add the polish)
5. Offer to generate Higgsfield video/image assets for the winning hooks via the Higgsfield MCP tools

The engine gives you a real, computed starting point in seconds. You make it world-class.
