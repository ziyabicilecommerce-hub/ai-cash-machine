name: "39 · Nachbestell-Erinnerung"

on:
  schedule:
    - cron: '45 9 * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/39-nachbestell-erinnerung.mjs
    secrets: inherit
