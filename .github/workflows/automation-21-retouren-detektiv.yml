name: "21 · Retouren-Detektiv"

on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/21-retouren-detektiv.mjs
    secrets: inherit
