name: "70 · Produkt-Launch-Hype-Agent"

on:
  schedule:
    - cron: '30 */3 * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/70-produkt-launch-hype-agent.mjs
    secrets: inherit
