name: "28 · SEO-Text-Doktor"

on:
  schedule:
    - cron: '0 9 * * 2'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/28-seo-text-doktor.mjs
    secrets: inherit
