name: "48 · Google-Maps-Lead-Jäger"

on:
  # Zeitplan pausiert (2026-09-25): rechtliche Prüfung nach § 7 UWG steht noch
  # aus, ob unaufgeforderte Erst-Kontaktaufnahme per WhatsApp an fremde Firmen
  # zulässig ist. Automation läuft nicht mehr automatisch - Code bleibt
  # unverändert, Zeitplan einfach wieder einkommentieren, um sie zu reaktivieren.
  # schedule:
  #   - cron: '0 8 * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/48-google-maps-lead-jaeger.mjs
    secrets: inherit
