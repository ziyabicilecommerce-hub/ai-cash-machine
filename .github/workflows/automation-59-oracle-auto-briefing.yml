name: "59 · Oracle Auto-Briefing"

on:
  schedule:
    - cron: '30 8 * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/59-oracle-auto-briefing.mjs
    secrets: inherit
