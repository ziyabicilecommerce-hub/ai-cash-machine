name: "31 · Umsatz-Prognose"

on:
  schedule:
    - cron: '0 20 * * 0'
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/31-umsatz-prognose.mjs
    secrets: inherit
