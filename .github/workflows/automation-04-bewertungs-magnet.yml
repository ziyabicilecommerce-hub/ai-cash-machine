name: "04 · Bewertungs-Magnet"

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/04-bewertungs-magnet.mjs
    secrets: inherit
