name: "61 · Pricing-Agent"

on:
  schedule:
    - cron: '45 6 * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/61-pricing-agent.mjs
    secrets: inherit
