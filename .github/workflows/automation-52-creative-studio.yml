name: "52 · Creative Studio"

on:
  schedule:
    - cron: '0 8 * * 3'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/52-creative-studio.mjs
    secrets: inherit
