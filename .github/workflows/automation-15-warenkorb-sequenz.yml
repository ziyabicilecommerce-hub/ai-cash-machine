name: "15 · Warenkorb-Sequenz 3.0"

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/15-warenkorb-sequenz.mjs
    secrets: inherit
