name: "09 · Newsletter-Autopilot"

on:
  schedule:
    - cron: '0 9 * * 4'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/09-newsletter-autopilot.mjs
    secrets: inherit
