name: "23 · Kunden-Jubiläum"

on:
  schedule:
    - cron: '30 10 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/23-kunden-jubilaeum.mjs
    secrets: inherit
