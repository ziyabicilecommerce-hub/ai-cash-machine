name: "90 · Social-Content-Autopilot"

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/90-social-content-autopilot.mjs
    secrets: inherit
