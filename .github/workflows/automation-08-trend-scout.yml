name: "08 · Trend-Scout"

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch: {}

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/08-trend-scout.mjs
    secrets: inherit
