name: "41 · Post-Purchase-Sofort-Upsell"

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/41-post-purchase-sofort-upsell.mjs
    secrets: inherit
