name: "02 · KI-Kundenservice"

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  # Für echte GitHub-Issue-Tickets bei Eskalationen (siehe lib/githubIssues.mjs)
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/02-ki-kundenservice.mjs
    secrets: inherit
