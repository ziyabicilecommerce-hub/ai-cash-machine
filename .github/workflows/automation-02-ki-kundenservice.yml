name: "02 · KI-Kundenservice"

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/02-ki-kundenservice.mjs
    secrets: inherit
