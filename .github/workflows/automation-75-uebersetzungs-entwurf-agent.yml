name: "75 · Übersetzungs-Entwurf-Agent"

on:
  schedule:
    - cron: '15 9 * * 2'
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

jobs:
  run:
    uses: ./.github/workflows/_automation-runner.yml
    with:
      script: automations/75-uebersetzungs-entwurf-agent.mjs
    secrets: inherit
